{
  "version": 2,
  "name": "syt-wallet-v1",
  "framework": "vite",
  "buildCommand": "rm -rf node_modules package-lock.json .vite dist && npm install && npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
